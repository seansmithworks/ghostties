# ghostties-web

Static marketing site for Ghostties (ghostties.org). Plain HTML/CSS/JS, no build step. Deployed on Vercel from `main` with `rootDirectory=web`.

The `appcast-beta.xml` and `appcast-stable.xml` files are generated by CI on each release tag — do not hand-edit them.
