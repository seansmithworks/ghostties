You are an adversarial plan reviewer. Your ONLY job is to refute the plan below. Default to skepticism: assume the plan is flawed and prove how. Do not be agreeable, do not summarize, do not compliment.

You have read access to the repository at /Users/seansmith/Code/ghostties/.claude/worktrees/slice-a. READ THE ACTUAL FILES. Do not take the plan's description of the codebase on faith, including its description of anything the planner authored. Verify its claims and cite path:line when you attack one.

DRAFT PLAN TO REFUTE: read it IN FULL from
/Users/seansmith/Code/ghostties/.claude/worktrees/slice-a/docs/plans/composer-ui-11/draft-plan.md
(217 lines). Read the whole file before writing anything.

ORIGINAL REQUEST (verbatim):
Build composer UI directions 11.1 and 11.2 overnight. 11.1 = ghosted full-path prefill at rest (whole path "Ghostties > Default > Orchestrator" ghosted in the field, predicted from last selection; the separate resolution line is DELETED). 11.2 = inline completion while typing ("Gho" solid + "stties" ghosted) plus pinned/recent/template rows carried WITHOUT section headers. Sean was shown the risk of the NSTextView rewrite explicitly and chose to build both tonight including model B. Work lands on a new branch feat/composer-ui-11 cut from feat/composer-branch-segment @ a1daa6608; the parent branch is FROZEN (it gates the beta.24 release on Sean's hands-on pass). This will be built AUTONOMOUSLY OVERNIGHT while Sean sleeps, so plan errors will not be caught by a human before code is written.

KEY RESEARCH FINDINGS the plan rests on (verify each):
- Deployment target is macOS 13.0 for the main app (macos/Ghostties.xcodeproj/project.pbxproj lines 892, 916, 939, 962, 992, 1022, 1052, 1303, 1365).
- Backport.onKeyPress is a NO-OP below macOS 14 (macos/Sources/Helpers/Backport.swift:53-68), so .onSubmit is the ONLY working Return handler on the shipped target. The rewrite must reimplement Return in AppKit.
- Zero NSViewRepresentable in the tree wraps text input; none use a Coordinator. NSTextView is never constructed anywhere. Closest precedent is TabTitleEditor.swift (field editor via currentEditor(), downcast to NSTextView).
- ComposerQueryField (SessionComposerPalette.swift:1762-1901) carries six SwiftUI-only behaviours with no AppKit equivalent: .focused($binding), .onExitCommand, .onMoveCommand, .onSubmit, .backport.onKeyPress, and four hidden zero-size .keyboardShortcut Buttons (up :1824, down :1827, ctrl-P :1831, ctrl-N :1834).
- Invariant (a): those four Buttons are CONDITIONALLY MOUNTED only when !isPickerOpen. A round-2 review fix. Leaving them mounted registered a duplicate shortcut pair against ProjectDropdownView.keyboardCaptureLayer.
- Invariant (b): double-fire of .onSubmit + .onKeyPress is harmless only because every ComposerOption.action nils selectedIndex SYNCHRONOUSLY.
- The focus-loss auto-dismiss was DELIBERATELY REMOVED (SessionComposerPalette.swift:1519-1523) because the project dropdown and NSOpenPanel take first responder.
- Template pinning DOES NOT EXIST. AgentTemplate (Models/AgentTemplate.swift:9-110) has no isPinned. Only Project has one.
- RecentComposerSelection (SessionComposerStore.swift:34-40) is (projectId, templateId) pairs with NO timestamp, JSON in UserDefaults key ghostties.sessionComposerRecentSelections, capped at 3, written only from precommit() :671.
- The list is four HEADED sections at SessionComposerPalette.swift:975-981 (RECENT/TEMPLATES/PROJECTS/COMMAND).
- "New template" is a footer Button OUTSIDE the list (:1287-1325), always visible, never scrolls.
- resolutionLine (:1140+, DESIGN.md:185-187) is the ONLY mouse entry point into the project/branch inline pickers, and renders three states nothing else does: "Default", "Creating...", and a .systemRed branch-not-found error.
- A TextField cannot tint sub-ranges at macOS 13; the styled-layer-behind-transparent-field workaround fails because a single-line field scrolls horizontally and SwiftUI exposes no way to read that offset.
- Repo failure mode: every second review round here has found a real defect the first missed, six-for-six, and fix commits have themselves regressed already-cleared paths.

STATED ASSUMPTIONS (the plan's own section 11, attack these hardest):
1. SwiftUI .keyboardShortcut hidden Buttons intercept keys via window-level performKeyEquivalent before an AppKit first responder's keyDown, for a plain NSTextView as they do for a focused TextField's field editor.
2. macOS standard key bindings translate ctrl-P/ctrl-N to moveUp:/moveDown: before textView(_:doCommandBySelector:).
3. NSLayoutManager.addTemporaryAttributes with .foregroundColor renders without invalidating layout on the TextKit stack a plain NSTextView(frame:) gets on macOS 13 through 26; construction forces textView.layoutManager access before first draw to guarantee compatibility mode.
4. Prefix-only inline completion is the correct reading of 11.2.
5. The prediction's rest-state source (head of recentProjectIds + last pair for that project) matches "predicted from the last selection".
6. Date encodes/decodes through the store's default JSONEncoder/JSONDecoder consistently.
7. Preset template ids remain deterministic across launches (PresetLoader.swift:162), making UUID-keyed pins stable.
8. The suite baseline at a1daa6608 is green.
9. xcodebuild needs ONLY_ACTIVE_ARCH=YES ARCHS=arm64 and derivedDataPath macos/build.
10. The .anchored (sidebar popover) presentation should adopt the new field automatically.

Return numbered findings (F1, F2, ...) under these exact headings:

1. WRONG. Claims, steps, or assumptions that are incorrect, risky, or will break. For each: what is wrong, the evidence, and the consequence if it ships as-is.
2. MISSING. Edge cases, dependencies, failure modes, constraints, surfaces, or steps that are absent. For each: what is missing and why it matters.
3. OVERVALUED / GOLD-PLATED. Parts that cost more than they are worth or buy scope the task does not need. For each: what to cut and why.
4. EVIDENCE. Attack how the plan gathered its facts, not only its logic. Which claims were never verified against a file? Which measurement could not have seen the thing it claims to have checked? Which stale source (memory file, summary, comment, prior report) is being trusted?
5. WEAKEST ASSUMPTION. The single load-bearing assumption most likely to be false, and what happens to the plan if it is.

Rules:
- Be specific. Cite the exact step or claim you are attacking. Vague criticism is useless.
- A few load-bearing kills beat a long list of nitpicks.
- A section may end "no material findings", but only after a real attempt to break it, never as a default.
- This plan will be executed autonomously overnight with no human checkpoint, so weight findings by what would silently produce broken or unreviewable work by morning.
- End with VERDICT: ship-as-is / revise / rethink, plus the single most important change.
